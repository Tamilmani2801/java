-It is based on xml based configuration where we used to return directly without using thymeleaf.
-In this project we need two xml file one for configuration fo spring which should be in the name of (project name )-servlet.xml and 
other is normal web.xml.